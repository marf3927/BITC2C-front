const initialState = {
    baseURL:"http://localhost:5555",
}

function auth(state, action) {
    if (state === undefined) {
        return initialState
    }
    let newState

    return newState
}
export default auth;